# requirements.txt

# This file lists the Python packages required to run the Secure RAG project.
# It is configured for Python 3.11 and CUDA 12.1.
# --- PyTorch with CUDA 12.1 Support ---
# This section ensures the correct GPU-enabled version of PyTorch is installed.
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.3.1+cu121
torchvision==0.18.1+cu121
torchaudio==2.3.1+cu121

# --- LangChain Core Framework ---
# Pinned to versions known to be compatible with the rest of the stack.
langchain==0.1.20
langchain-core==0.1.52
langchain-community==0.0.38
langchain-huggingface==0.0.3
langchain-chroma==0.1.1

# --- Core AI & LLM Libraries ---
# The primary engines for embeddings, extraction, and generation.
transformers==4.41.2
sentence-transformers==2.7.0
llama-cpp-python>=0.2.70
accelerate

# --- Data Handling & Document Loading ---
pymupdf
unstructured[docx,md]
chromadb==0.5.0
nltk

# --- Utility & Dependency Fixes ---
# These are specific versions pinned to resolve known compatibility issues.
python-dotenv
packaging==23.2          # Pinned for langchain-core compatibility
async-timeout==4.0.3     # Pinned for langchain compatibility
posthog<4                # Pinned for chromadb compatibility